#include <stdio.h>
#include <stdlib.h>
int main()
{
	int arr[]={6,7,8,9,10,11,13,14,16,17};
	int n=11,len;
	while(i<=len)
	{
		if(arr[i] - i != difference){
			while(difference < arr[i] - i)
			{
				printf("%d\n", i + difference);
				difference++;
			}

		return 0;
}
